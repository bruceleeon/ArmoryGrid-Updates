{
  "version": "1.1.12.4",
  "containerImage": "ghcr.io/bruceleeon/armory-grid:1.1.12.4",
  "notes": [
    "Fixed authenticated private GHCR image pulls in the self-updater by sending a correctly padded Docker X-Registry-Auth header.",
    "Update failures now surface the updater's real Docker/GHCR error instead of only a generic HTTP 500.",
    "Retains the Configurable Lists card redesign and removal of the top-level Load JSON button.",
    "No inventory schema changes and no destructive data migration."
  ],
  "windows": {
    "packageUrl": "",
    "sha256": ""
  }
}
